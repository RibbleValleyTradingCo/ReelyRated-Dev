[auth]
site_url = "http://localhost:8080"

additional_redirect_urls = [
  "http://localhost:8080/auth?type=recovery"
]

# Delete / comment these:
# [auth.email]
#
# [auth.email.smtp]
# enabled = true
# host = "mailpit"
# port = 1025
# user = ""
# pass = ""
# admin_email = "no-reply@localhost"
# sender_name = "ReelyRated (Local Dev)"